name: ci

on:
  push:
    branches: [ main ]
    tags:
      - "v*.*.*"
  # pull_request:
  #   branches:
  #     - 'main'

jobs:

  docker:
    runs-on: ubuntu-latest

    steps:

      - name: Test
        run: echo ${{ GITHUB_REF }}
  
    # https://github.com/actions/checkout
    # - name: Git Checkout
    #   uses: actions/checkout@v2

    # - name: Get Tag 
    #   id: get_tag
    #   run: echo "##[set-output name=version];"

    # - name: Setup QEMU
    #   uses: docker/setup-qemu-action@v1

    # - name: Setup Docker Buildx
    #   id: buildx
    #   uses: docker/setup-buildx-action@v1

    # - name: Login to Docker Hub
    #   if: github.event_name != 'pull_request'
    #   uses: docker/login-action@v1
    #   with:
    #     username: ${{ secrets.DOCKER_HUB_USERNAME }}
    #     password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

    # - name: Login to GHCR
    #   if: github.event_name != 'pull_request'
    #   uses: docker/login-action@v1
    #   with:
    #     registry: ghcr.io
    #     username: ${{ github.repository_owner }}
    #     password: ${{ secrets.GHCR_TOKEN }}

    # - name: Cache Docker Layers
    #   uses: actions/cache@v2
    #   with:
    #     path: /tmp/.buildx-cache
    #     key: ${{ runner.os }}-buildx-${{ github.sha }}
    #     restore-keys: |
    #       ${{ runner.os }}-buildx-

    # - name: Build and Push Latest Only
    #   if: github.ref == 'refs/heads/main'
    #   id: docker_build_latest
    #   uses: docker/build-push-action@v2
    #   with:
    #     context: ./
    #     file: ./Dockerfile
    #     builder: ${{ steps.buildx.outputs.name }}
    #     push: ${{ github.event_name != 'pull_request' }}
    #     tags: |
    #       digitalmine/docker-mssql:latest
    #       ghcr.io/${{ github.repository_owner }}/docker-mssql:latest 
    #     cache-from: type=local,src=/tmp/.buildx-cache
    #     cache-to: type=local,dest=/tmp/.buildx-cache

    # - name: Build and Push Tags and Latest
    #   if: github.ref != 'refs/heads/main'
    #   id: docker_build_all
    #   uses: docker/build-push-action@v2
    #   with:
    #     context: ./
    #     file: ./Dockerfile
    #     builder: ${{ steps.buildx.outputs.name }}
    #     push: ${{ github.event_name != 'pull_request' }}
    #     tags: |
    #       digitalmine/docker-mssql:$(echo ${ GITHUB_REF:10 })
    #       ghcr.io/${{ github.repository_owner }}/docker-mssql:$(echo ${ GITHUB_REF:10 })
    #     cache-from: type=local,src=/tmp/.buildx-cache
    #     cache-to: type=local,dest=/tmp/.buildx-cache

    # - name: Image Digest
    #   run: echo ${{ steps.docker_build.outputs.digest }}
